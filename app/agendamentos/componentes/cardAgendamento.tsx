import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";

const cardAgendamento = () => {
  return (
    <Card>
      <div>
        <Button className="rounded-full">Confirmado</Button>
      </div>
    </Card>
  );
};

export default cardAgendamento;
